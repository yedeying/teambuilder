.folder-list
  .title-block
    i.fa.fa-file
    h4 文件夹列表
    span.manage-folder 管理
  each folder in data.folderList
    .line(class=(folder.fid === data.defaultFolder ? 'on' : ''), data-fid=func.sha1(folder.fid.toString()))
      span.folder #{folder.name}
      span.span= folder.fileList.length